# full / partial 条款真机验收清单

> 由当前 `framework/clause_tool_map.json` 生成；仅列 full/partial，manual 条款进入报告手工指引。

| 条款 | 自动化等级 | 当前工具类别 | 当前方法 | 签收 |
|---|---|---|---|---|
| 5.1-1 | partial | bash: nmap; playwright_mcp: browser_navigate / browser_snapshot / browser_type / browser_click | Nmap full-port scan vs IXIT; multi-device password comparison; playwright MCP drive init flow to test password skip/weak-password enforcement | ☐ |
| 5.1-2 | partial | 既有产物/人工边界 | Compare actual passwords against IXIT generation-mechanism description (length, charset, uniqueness) | ☐ |
| 5.1-3 | full | bash: nmap; playwright_mcp: browser_navigate / browser_snapshot / browser_fill_form / browser_click / browser_network_requests / browser_network_request / browser_evaluate | Read pcap_analysis TLS version/cipher JSONs; nmap ssl-enum-ciphers; playwright frontend encryption check per frontend-encryption-check.md; compare with IXIT | ☐ |
| 5.1-4 | partial | playwright_mcp: browser_navigate / browser_fill_form / browser_click / browser_evaluate / browser_network_requests | Playwright MCP: login → navigate to password-change page → fill old+new+confirm → submit → verify POST 200 → logout → verify new password works and old password fails | ☐ |
| 5.1-5 | partial | bash: nmap; burp_mcp: intruder_send | Nmap full-port scan to confirm auth interfaces; Burp Intruder replay login requests (encrypted or plaintext payload); observe lockout threshold and duration vs IXIT declaration; 1-2 failed probes only, full lockout test marked manual | ☐ |
| 5.2-1 | full | bash: curl | curl anonymous access to vulnerability disclosure page; check: (1) contact info present (2) acknowledgement timeline (3) status-update timeline | ☐ |
| 5.2-2 | partial | bash: python | 从 3-VulnTypes 逐类提取披露漏洞的 Action 和 Time Frame，并回查 2-UserInfo 的漏洞披露政策；检查两者是否共同支持及时处理，再核对 4-Conf 的漏洞行动确认。对严重性、漏洞类型、第三方依赖和责任分工只能依据 IXIT 已声明信息判定。 | ☐ |
| 5.2-3 | partial | bash: python | 从 5-VulnMon 分别提取持续监测、影响识别和修复/缓解方法，检查是否形成面向 DUT 的闭环；再核对 4-Conf 的漏洞监控确认。不得把单一公告订阅或“定期关注”文字视为持续监测闭环。 | ☐ |
| 5.3-1 | partial | 既有产物/人工边界 | Python extract IXIT JSON → iterate 6-SoftComp mechanisms → aggregate evidence (5.3-2-2 direct + 5.3-9/5.3-10 indirect + observation) → LLM per-mechanism judgment on abuse potential | ☐ |
| 5.3-2 | full | burp_mcp: burp_import_project_config / proxy_history / proxy_websocket_history / websocket_list / websocket_get_messages | (1) TLS downgrade MITM: burp_import_project_config with custom_tls_protocols=[TLSv1,TLSv1.1] + enable_http2=false → trigger DUT update → observe if weak TLS accepted → restore config. (2) Firmware tampering: Burp proxy hijack update request; upload old/tampered firmware → verify rejection; interrupt transfer → verify recovery. WebSocket flow: proxy_history find /updateFirmware + status 101 → websocket_list → websocket_get_messages direction=server_to_client max_results<=20 | ☐ |
| 5.3-3 | partial | bash: python | 从 ICS.required_ixit_entries、6-SoftComp 与 7-UpdMech 按组件建立更新机制清单；逐组件检查至少一条更新路径、用户可识别的操作入口和 IXIT 描述是否自洽。机制未登记、描述无法说明如何更新或组件无更新路径 → FAIL；缺少组件—机制对应关系或可用性细节 → INCONCLUSIVE。 | ☐ |
| 5.3-4 | partial | bash: python; playwright_mcp: browser_navigate / browser_snapshot | 先从 ICS 与 7-UpdMech 判断是否支持自动更新；不支持则记录前提不满足并标 N/A。支持时，检查每个组件至少存在一种无需用户交互的检查/更新机制，并用 Playwright snapshot 复核更新设置页的自动检查或自动安装状态；可配置时默认至少启用一种自动机制。 | ☐ |
| 5.3-5 | partial | bash: python; burp_mcp: proxy_history; playwright_mcp: browser_navigate / browser_snapshot | 从 7-UpdMech 提取检查触发者、初始化后行为与周期；Traffic 操作窗触发一次“检查更新”，用 Burp history 索引该请求并在 UI snapshot 中复核设置。DUT 自触发、初始化后检查且有周期说明才可 PASS；只有用户手动检查、缺少周期或没有可索引请求时不得补写为 PASS。 | ☐ |
| 5.3-7 | full | bash: nmap; burp_mcp: proxy_history | 从 Burp history 定位更新相关流量；读取 pcap 的 ServerHello/协议分层，并以 nmap ssl-enum-ciphers 佐证 DUT 支持值；逐项比对 7-UpdMech/11-ComMech/12-NetSecImpl 声明的 TLS 版本、密码套件、证书和签名算法。元数据查询或普通 Web 页面 TLS 不能替代更新载荷证据。 | ☐ |
| 5.3-8 | partial | bash: python | 按组件读取更新机制、部署通道与确认机制；概念性判断管理程序是否允许及时部署安全更新并确认执行。仅有“支持升级”但未说明部署路径、时效或确认机制时标 INCONCLUSIVE。 | ☐ |
| 5.3-11 | partial | bash: python; burp_mcp: proxy_history; playwright_mcp: browser_navigate / browser_snapshot | 先判定是否存在自动检查更新的前提；前提不成立且 ICS 为 N/A 时标 N/A。前提成立时，读取 7-UpdMech 的通知描述，并用 UI snapshot 复核通知入口/设置；通知必须可识别、明显，且含所减轻安全风险的信息。“用户可手动检查”不构成通知。 | ☐ |
| 5.3-12 | partial | bash: python; burp_mcp: proxy_history; playwright_mcp: browser_navigate / browser_snapshot | 从 7-UpdMech 判断更新是否会中断基本功能；若明确不会中断，则按 escape clause 标 PASS 并引用该字段。若会中断，读取/抓取 UI 中的中断提示，检查提示是否在 DUT 上可见且明确告知基本功能中断。 | ☐ |
| 5.3-6 | partial | playwright_mcp: browser_navigate / browser_snapshot / browser_click | Playwright MCP: login → navigate to system maintenance / firmware upgrade page → browser_snapshot to find auto-update toggles (auto-check/auto-download/auto-install/notify-new-version) → record each toggle default state (ON/OFF) → browser_click to toggle → verify modifiable | ☐ |
| 5.3-9 | partial | burp_mcp: proxy_history / websocket_get_messages | Upload tampered firmware → observe rejection; offline verification. Firmware upload traffic query: Burp proxy_history → WebSocket → websocket_get_messages direction=server_to_client max_results<=20 | ☐ |
| 5.3-10 | full | bash: nmap | Verify update trust relationship: IXIT document analysis for mTLS/Token/user-confirmation auth-entity verification; Nmap full-port scan for hidden update interfaces → compare with IXIT 7-UpdMech | ☐ |
| 5.3-13 | full | bash: curl | curl access public notice page → check: (1) accessible (2) no registration required (3) support period published | ☐ |
| 5.3-14 | full | bash: curl | curl access page → check: (1) reason stated (2) replacement plan (3) timeline (4) no registration required | ☐ |
| 5.3-16 | partial | bash: nmap; playwright_mcp: browser_navigate / browser_snapshot | Playwright MCP: browser_navigate DUT Web UI homepage/system info → browser_snapshot extract device model → compare with nmap HTTP title/SSL cert CN → compare with IXIT model declaration. Output three-column comparison table: Web UI \| nmap \| IXIT → consistent = PASS | ☐ |
| 5.5-1 | full | bash: nmap; playwright_mcp: browser_navigate / browser_snapshot / browser_fill_form / browser_click / browser_network_requests / browser_network_request / browser_evaluate | Read pcap_analysis JSONs (server_hello, client_hello, proto_hierarchy, conversations, http_check, http_responses); nmap ssl-enum-ciphers; playwright MCP frontend encryption check per frontend-encryption-check.md; downgrade attack testing; compare with IXIT | ☐ |
| 5.5-2 | partial | bash: curl | Python extract IXIT 12-NetSecImpl Review/Evaluation Method + Report → output filled/unfilled list → if Report is URL, curl verify reachability → manual review of assessment evidence coverage + DUT version vs Report consistency | ☐ |
| 5.5-3 | partial | bash: python | 筛出 6-SoftComp 中标记 Cryptographic Usage 的每个组件，逐一索引其 Update Mechanism，并检查厂商是否记录算法/原语升级后的兼容性、副作用或迁移处理。加密组件随已受控的固件更新可整体更新可作为机制佐证，但不能推断实际已完成算法迁移。 | ☐ |
| 5.5-4 | full | burp_mcp: auth_diff / proxy_latest_auth / auth_scan_endpoints; playwright_mcp: browser_navigate / browser_snapshot / browser_fill_form / browser_click / browser_network_requests / browser_network_request / browser_evaluate | (1) Auth control: 先 mcp__burp__proxy_latest_auth{host:<DUT>} 取双头(Cookie+SessionTag 或启发式认证头, 同帧避免配对错)→ auth_diff 全端点 authenticated vs none。auth_diff 新参数: headers:[{name,value}] 多头数组(替代单头 header_name/header_value); device_hint:"isapi" 开启双头校验(base 有 SessionTag 且 level 无 Cookie → AUTH_DIFF_DUAL_HEADER_REQUIRED); preserve_headers 逃生舱(保留认证头不剥); 输出含 auth_headers_stripped 明示剥头。none returns 200+body match = FAIL, 401/403 = PASS. (2) Encryption verification: playwright MCP frontend encryption analysis (same as 5.1-3 flow) + pcap_analysis server_hello TLS cipher/cert vs IXIT comparison. Both PASS → 5.5-4-2 PASS | ☐ |
| 5.5-5 | full | bash: nmap; burp_mcp: auth_diff / proxy_latest_auth / auth_scan_endpoints; playwright_mcp: browser_navigate / browser_snapshot / browser_fill_form / browser_click / browser_network_requests / browser_network_request / browser_evaluate | A+B+Port: (1) 先 mcp__burp__proxy_latest_auth{host:<DUT>} 取认证头→ auth_diff 全端点 admin vs none(evidence A, per auth-diff-workflow.md)。auth_diff 新参数: headers 多头数组 + device_hint:"isapi" 双头校验 + preserve_headers 逃生舱 + auth_headers_stripped 明示。(2) Frontend encryption logic vs IXIT consistency (evidence B, per frontend-encryption-check.md) (3) nmap full-port vs IXIT 15-Intf. All three PASS → 5.5-5-2 PASS | ☐ |
| 5.5-6 | partial | bash: python | 从 10-SecParam 索引关键安全参数及其 11-ComMech 通信机制，读取 pcap 的 DUT 外连、非 TLS、HTTP 明文、TLS 应用数据和敏感关键字结果；将每条实际通信归属到 IXIT 机制后，再比对机密性与加密细节。零外连不等于没有关键安全参数通信。 | ☐ |
| 5.5-7 | partial | bash: python | 从 10-SecParam 与 11-ComMech 筛选远程可访问的关键安全参数通信，读取 remote_ips/all_destinations 和非 TLS 分析结果；将远程地址、协议和 TLS 细节与 IXIT 声明逐项关联。没有远程 CSP 通信才可 N/A，不能只因 remote_ips 为空就推断。 | ☐ |
| 5.5-8 | partial | bash: python | 从 14-SecMgmt 对每类关键安全参数核对生成、分发、存储、更新、停用/归档/销毁及过期/泄露处理，再核对 4-Conf 的安全管理确认。生命周期缺口必须逐项报告，不能用“有密钥管理”笼统判定。 | ☐ |
| 5.6-1 | full | bash: nmap | Nmap full-port scan → compare with IXIT 15-Intf Enable/Disable status | ☐ |
| 5.6-2 | full | bash: ncat / curl; burp_mcp: http_fuzz | Read pcap_analysis JSONs: broadcast.json (broadcast/multicast traffic → info-leak channels), plaintext_services.json (non-TLS non-HTTP TCP plaintext banners → version/SN leak); ncat banner collection; curl unauthenticated paths; Burp directory brute force | ☐ |
| 5.6-5 | full | bash: netstat | netstat list listening services → compare with IXIT 13-SoftServ for unexpected default services | ☐ |
| 5.6-6 | partial | bash: python | 提取 16-CodeMin 中全部代码最小化技术，按“减少到设备必需功能”的目标检查其适用范围、实施方式与是否覆盖未使用/不需要代码。只可评价声明的技术逻辑，不能从网络扫描结果推断二进制中不存在死代码。 | ☐ |
| 5.6-7 | partial | bash: python | 逐项读取 17-PrivlCtrl 的软件权限控制机制，评估其是否共同支持职责分离、知其所需与最小权限；明确把特权进程、非特权服务、隔离/能力限制等描述关联到对应组件。不得以“需要 root”一语自动判 FAIL，必须看是否有最小化限制。 | ☐ |
| 5.6-8 | partial | bash: python | 读取 18-AccCtrl 中每个内存访问控制机制，分别判断其是否在硬件层实现（包括嵌入硬件的固件）以及是否能控制内存访问；将 MMU/MPU/NX/TEE 等机制与其控制对象和边界明确关联。 | ☐ |
| 5.6-9 | partial | bash: python | 按 19-SecDev 的流程核对安全培训、需求/设计、安全编码、实现工具、安全测试、安全审查、资产归档、安全部署，以及适用时第三方供应商处理；再核对 4-Conf 的安全开发确认。逐项标注已声明、明确缺失或描述不足，不能把一份政策标题当作覆盖全部阶段。 | ☐ |
| 5.8-1 | partial | bash: python | 从 21-PersData 逐类索引个人数据及引用的 11-ComMech，复用 5.5-1 的 TLS/明文证据并比对 12-NetSecImpl；必须证明所捕获通信确实承载对应个人数据，不能将普通流配置参数或无关 Web 流量当作个人数据证据。 | ☐ |
| 5.8-2 | partial | 既有产物/人工边界 | IXIT 21-PersData per-category verification: (1) conceptual assessment of encryption algorithm security (AES/TLS/SHA256 known-feasible-attack check) (2) functional verification per-category data transmission encryption: read pcap_analysis proto_hierarchy.txt + server_hello.json (confirm encryption layer covers corresponding protocol, not stream config metadata). Stream channel config params (codec/resolution/bitrate) are NOT sensitive personal data. No actual business triggered (email/face/POS, RTP video) → INCONCLUSIVE | ☐ |
| 5.8-3 | full | bash: curl | curl access sensor documentation → readability assessment → visual sensor inspection | ☐ |
| 5.9-3 | partial | bash: python | 对 11-ComMech 的每种通信机制读取 Resilience Measures，再读取 SYN 突发、时间间隔和重传分析；检查有序连接和防止大规模同步重连的证据是否与 IXIT 一致。普通稳定通信中的无 SYN 突发不能证明断连后的回退策略。 | ☐ |
| 5.12-1 | partial | playwright_mcp: browser_navigate / browser_snapshot / browser_click | Playwright MCP: login → click Configuration Wizard (or factory-reset init flow) → per-page browser_snapshot to extract each decision point → check: (1) default pre-checked boxes? (2) terminology understandable? (3) Skip button present? (4) privacy/TOS links available? → compare with IXIT 26-UserDec | ☐ |
| 5.12-2 | partial | 既有产物/人工边界 | Configure DUT per security settings documentation → check security decision coverage | ☐ |
| 5.12-3 | full | bash: curl | curl access manufacturer documentation → check if security configuration self-check guidance provided (checklist items / auto-check tools / security status indicators) | ☐ |
| 5.13-1 | full | bash: sqlmap / xray / nmap / python; burp_mcp: proxy_latest_auth / sqlmap_targets / sqlmap_bridge_run / sqlmap_batch_run / sqlmap_bypass_retry / sitemap_query / http_fuzz / repeater_send | 先调 mcp__burp__proxy_latest_auth{host:<DUT>} 一步拿最新认证头(扩展内真频率分析选帧 + 同帧提取 tokens{cookie,session_tag,authorization} + auth_headers_detected + live GET 验证, verify.valid=true 即有效; 换认证头名的设备也能被启发式发现)。不再用 session_ensure.py 两步法。再调 mcp__burp__sqlmap_targets{host:<DUT>, auth_headers:{...proxy_latest_auth tokens}} 批量筛 sqlmap 可扫端点(扩展内 sitemap 采集 → 静态/preauth/404/空过滤 → (method,path) 去重 → 参数提取+打分 HIGH/SKIP → live 动态预检改值响应变化→提升 HIGH → 输出 sqlmap_manifest.json, 每端点带已注入新认证头的 request 原文)。认证头注入以下测试 → (1) Path discovery: python scripts/dir_brute_force.py {workspace} --target=<DUT> --label=5.13-1 生成 mcp__burp__http_fuzz 顶层路径爆破指令 (内置 IoT/Web 词表, 经 Burp 代理认证天然带), 非 404 命中 → --ingest 筛 hits, 目录型命中 → --base=<path> 递归子路径深挖 + sitemap_query API endpoint extraction (auto-deduplicated). (2) Parameter injection: http_fuzz on discovered endpoints with FUZZ markers for SQLi/XSS/command payloads in params/Cookie/Body (基于 manifest.request 原文, 已带认证头). (3) Deep injection: sqlmap 三工具族 per web-sqli.md: sqli_probe.py 消费 sqlmap_manifest.json 把 request 原文写 -r 请求文件并输出 bridge_calls[]/batch_calls[]/bypass_calls[] → 单端点快扫 mcp__burp__sqlmap_bridge_run{workspace, endpoint_id:<seq>, mode:quick}；多端点批量 mcp__burp__sqlmap_batch_run{endpoint_ids:1,2,3, mode:quick, escalate:true, batch_delay_ms} 一批全跑+quick→deep 自动升级+限速；WAF/时间盲注疑似被拦端 → mcp__burp__sqlmap_bypass_retry{endpoint_id, max_attempts} 沿 tamper 链(space2comment→between→charencode→…)迭代绕过, 每次尝试追加 sqlmap/block_ledger.jsonl。三工具统一带内置 auth 刷新(auth_refresh=true: 401/403 信号 → proxy_latest_auth 重取头 → 重写 .req → 重跑, 免手工预检)与四态 triage(confirmed/clean/suspect/blocked)。扩展内复刻 sqlmap4burp++ 启动逻辑: 读 manifest request → 写 .req → 同步跑 sqlmap --output-dir → 解析 sqlmap 自身日志回态, 注入点 payload 取自日志 → status:found/confirmed 再 mode:deep 深扫 → 注入点证据进 evidence_M5 → Burp Repeater 复核; 数据提取(--dbs/--tables/--dump)由 M5 agent 协调直调 sqlmap。 (4) Broad review: xray passive scan + nmap port vs API comparison. http_fuzz 路径爆破空 (dir_brute_force.py hits=0) + sitemap no DUT entries → INCONCLUSIVE | ☐ |
| 6-1 | full | bash: curl | curl access personal data documentation → content completeness + readability | ☐ |
| 6-2 | partial | 既有产物/人工边界 | Walk through consent flow: verify free + conspicuous + explicit consent; pre-checked boxes / default consent = FAIL | ☐ |
| 6-3 | partial | 既有产物/人工边界 | Walk through consent withdrawal flow → verify consistency with IXIT; account deletion ≠ authorization revocation | ☐ |
| 6-4 | partial | bash: python | 逐项读取 24-TelData 的遥测数据、处理目的及其引用的 21-PersData 个人数据，判断该数据是否为实现所述预期功能所必需；必须对每种遥测数据单独给出目的—必要性关系，不能用“改进体验/用于分析”等泛化目的直接判 PASS。 | ☐ |
| 6-5 | full | bash: curl | curl access telemetry documentation → content completeness check | ☐ |

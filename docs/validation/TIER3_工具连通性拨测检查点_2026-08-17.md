# Tier 3 工具全量连通性拨测 · 检查点（2026-08-17，/compact 前落盘）

> **状态：已完成 ✅ 全量测毕，完整矩阵已写入 `../TIER3_PROGRESS.md` §七（111✅ / 4⚠️ / 10❌Pro / 8🔸惰性 / 6⏭跳过 = 139）。**
> 本文件仅保留拨测过程细节，最终结论以 ../TIER3_PROGRESS.md §七 为准。
> 任务：139 个 burp MCP 工具全量拨测 → 结果矩阵写入 ../TIER3_PROGRESS.md。
> 环境：BurpMCP-Ultra 2.0.1 × Burp Community 2026.4.3（CE）。

## 已实测完成（约 56 个，全部 ✅ 除标注）

### Tier3 新工具（5/5 ✅）
proxy_latest_auth / auth_scan_endpoints / auth_diff（5 项冒烟全过）

### analyze（7/7 ✅）
analyze_request / analyze_response / analyze_diff / analyze_extract_params /
analyze_find_reflected / analyze_insertion_points / analyze_response_body_search

### HTTP 核心（✅）
http_send_request / http_send_request_chain / repeater_send / passive_intel /
http_analyze_keywords / http_analyze_variations / http_cookie_jar_get

### burp/项目（✅）
burp_version / burp_command_line_args / burp_export_project_config /
burp_export_user_config / burp_task_engine_set / burp_task_engine_state /
project_info / extension_info / ai_status（enabled:false）

### api（✅）
api_import_openapi（⚠️ 会加 scope include + sitemap，有残留）

### bcheck/scancheck/scanner（✅ 除 Pro 标注）
bcheck_templates ✅ / bcheck_list ✅ / bcheck_create ❌Pro / bcheck_import ❌Pro
scancheck_templates ✅ / scancheck_list ✅ / scancheck_create_passive ✅ /
scancheck_create_active ✅ / scancheck_remove ✅
scanner_get_all_issues ✅ / scanner_generate_report ✅ / scanner_register_check ✅ /
scanner_task_list ✅ / scanner_start_audit ❌Pro / scanner_start_crawl ❌Pro /
scanner_import_bcheck ❌Pro（BChecks API null）

### session/config/events/state（✅ 除 bug 标注）
session_create_token_rule ✅ / session_remove_rule ✅
config_proxy_listeners_list ✅ / config_match_replace_list ✅
config_match_replace_add ⚠️【疑似 bug】/ config_proxy_listener_add ⚠️【疑似 bug】
events_get ✅
intruder_send ✅ / intruder_register_payload_processor ✅
util_base64_encode ✅ / util_hash ✅
persistence_get ✅ / preference_get ✅
proxy_history ✅ / proxy_intercept_status ✅ / proxy_list_rules ✅
scope_get_config ✅ / websocket_list ✅ / organizer_get_items ✅ / organizer_send ✅
sitemap_query ✅ / sitemap_add_request ✅ / log_event ✅ / decoder_send ✅ / comparer_send ✅

## 🐛 新发现 bug（不在 pending-issues 里的真病灶）

**config_match_replace_add / config_proxy_listener_add 声称成功但实际未生效：**
- `config_match_replace_add` 返回 `{"status":"added","total_rules":13}`，但 `config_match_replace_list` 仍显示 12 条（0-11），新规则不可见
- `config_proxy_listener_add` 返回 `{"status":"added","total_listeners":2}`，但 `config_proxy_listeners_list` 仍显示 1 个（8080）
- 且无法删除：`config_match_replace_remove(index:12)` → "Invalid rule index: 12. Valid range: 0..11"；
  `config_proxy_listener_remove(127.0.0.1:18099)` → "No listener found"
- 判断：add 工具改的是内存副本或计数，未持久化到 Burp 配置。**待核验后加入 tool-error-kb.json**

## 已确认 Pro 门控（18 个）
collaborator_*(6) / scanner_start_audit / scanner_start_crawl / scanner_task_add_request/
delete/issues/status(4) / scanner_import_bcheck / bcheck_create / bcheck_import /
http_fuzz(超时) / http_race(超时) / ai_prompt

## 已产生残留（无法自动清理，需用户手动）
1. sitemap 条目：`http://127.0.0.1/tier3-smoke`（sitemap_add_request）
2. organizer 条目：`GET /tier3-smoke`（organizer_send）
3. scope include：`http://127.0.0.1:1/`（api_import_openapi 自动加 scope）
4. 报告文件：系统临时目录中的 `tier3-smoke-report.html`
5. decoder/comparer tab（无害）

## 剩余未测（下一步继续）
events_get_by_type / events_subscribe / events_unsubscribe / events_clear【跳过·清空缓冲区不可逆】
http_cookie_jar_set / http_list_traffic_rules / http_send_raw_bytes /
http_send_requests_parallel / http_set_traffic_rule / http_remove_traffic_rule
intruder_send_with_positions
websocket_create / websocket_get_messages / websocket_send_binary/send_text【跳过·往活动连接灌数据】/
websocket_close【跳过·会断用户 bilibili 连接】/ websocket_set_intercept_rule
persistence_store / persistence_list / persistence_delete
preference_store【残留·无删除工具】
log_message
proxy_annotate / proxy_auto_auth / proxy_history_search / proxy_intercept_enable/disable /
proxy_set_request_rule / proxy_set_response_rule / proxy_remove_rule /
proxy_websocket_history / proxy_websocket_history_search
scanner_create_issue / scanner_task_add_request/delete/issues/status / sitemap_add_issue / sitemap_get_issues
scope_check / scope_include / scope_exclude【残留】/ session_list_rules
util_base64_decode / util_compress / util_decode_smart / util_decompress / util_html_encode /
util_jwt_decode / util_random_bytes / util_random_string / util_shell_execute /
util_url_decode / util_url_encode
collaborator_generate_payload / collaborator_poll / collaborator_server_info /
collaborator_get_secret / collaborator_restore_client
bcheck_remove
burp_import_project_config / burp_import_user_config【可测·identity 往返】
config_upstream_proxy_set【跳过·无撤销会弄断代理】

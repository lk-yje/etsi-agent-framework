# 错误教训与回归台账

> 本文件记录已经实际发生、且通过运行或审计证实的问题。它不是设计愿望清单；每条都应保留触发条件、风险、纠正原则和回归条件。

## 2026-08-19：离线概念性测试的输出归属与 Agent 工具边界

### 触发场景

使用本地 `E:\Hikvision\ETSI_ICS_IXIT_NVR_303645-en.xlsx` 进行非正式离线测试：

- 现有 `scripts/parse_ixit_xlsx.py` 成功解析出 78 条 ICS、29 张 IXIT 表；
- M0 evidence 经真实 DeepSeek Auditor 审计后被 `REJECT`，发现条件性 `N/A`、自检计数和轨迹等问题；
- 以 M1 phase_B 的实际知识装配方式单测概念性条款 5.6-6。

5.6-6 Work Agent 正确读取了 `16-CodeMin` 与 ICS，形成了包含 IXIT 引用、理由、手工边界和 execution trace 的 `EvidenceManifest`。但它在 30 次工具调用中自行调用 `write_file`，将文件写成：

```text
evidence/pre-M1-5.6-6-evidence.json
```

而 PhaseEngine/L1 的正式交接路径是：

```text
Agent final JSON → PhaseEngine 验证/归并 → evidence/pre-M1-phase_B-evidence.json → evidence/pre-M1-evidence.json
```

结果是：该 JSON 单独用 Pydantic 可验证，但正式 L1 查找 `pre-M1-evidence.json` 时失败；模型绕开了 PhaseEngine 的归并、条款计数、审计触发和原子写入边界。

### 根因

1. `phase_B.tools=["bash", "burp_mcp"]` 中的 `bash` 是一个泛化类别，当前会解析出 `bash`、`python_script`、`write_file` 等标准工具；它并不只表示只读 shell。
2. Work Agent Prompt 既要求返回 JSON，又允许写文件，形成两个竞争输出通道。
3. 概念性条款没有专用的、多 IXIT 表受限检索工具；模型为读取 `ixit.json` 被迫使用泛化工具，并在过程中自行管理临时/最终文件。
4. PhaseEngine 期望从 `AgentRunner.raw_text` 获得最终 JSON，但未把“最终 JSON 是唯一正式交接物”收紧为工具策略。

### 必须澄清：不是禁止 Agent 产出 evidence

**Agent 仍然是 evidence 的作者。**它必须：

- 阅读 IXIT / ICS 和经授权的工具回执；
- 形成条款级 `reason`、`expected/actual`、IXIT 引用、证据等级、warnings、manualSteps 和 execution trace；
- 返回完整 `EvidenceManifest` JSON，作为它对执行过程的可审计陈述。

要禁止的是 Agent **自行写入正式 evidence 文件**。正式文件应由编排器接收 Agent 返回的 JSON 后：

1. 用 `EvidenceManifest` Pydantic 合约验证；
2. 检查本 phase 的条款范围、执行轨迹、证据级别和工具回执；
3. 原子写入唯一规范路径；
4. 由 PhaseEngine 合并 phase 文件为 `pre-Mx-evidence.json`，再交 L1/L2 和令牌闸门。

这样既保留 Agent 对过程的知情与署名，也防止它绕过文件命名、覆盖保护、phase 归并和证据链校验。类比为：检测员填写原始记录，LIMS/编排器负责校验、编号、归档；不是让检测员失去记录权。

### 风险

- 同一模块出现多个不受控 evidence 文件，报告可能漏计或重复计；
- 未经过 L1 的 JSON 被误当成正式证据；
- Agent 可覆盖旧文件或使用与本 phase 不匹配的命名；
- 模型的工具循环被本地文件管理消耗，挤占条款分析与最终结构化输出预算；
- 泛化 bash/python 工具扩大了概念性测试的非必要执行面。

### 纠正原则与后续实施

1. **双通道分工固定**：Agent 是证据内容作者，返回 EvidenceManifest；编排器验证并写正式 manifest 文件。禁止的是 Work Agent 直接写入最终命名的 `evidence/pre-Mx*.json`，不是禁止它形成证据内容。
2. **原始材料与正式 manifest 分层**：获准工具仍可在受控路径产生扫描输出、PCAP 摘要等原始材料，并留下 tool receipt；Agent 必须在 manifest 中引用这些材料。只有最终 clause manifest 的命名、原子写入和归并权归 PhaseEngine。
3. **概念性工具最小化**：提供只读 `read_ixit_table`、`read_ics_clause`、`search_ixit`（支持多个表、JSON Pointer 和带偏移的可引用结果）及只读 receipt 查询；概念性 phase 不开放通用 `write_file`/`python_script`。
4. **正式输出唯一**：PhaseEngine 只接受与本 phase clause_ids 完全对应的返回 JSON，并原子写入 `pre-Mx-phase_Y-evidence.json`；合并文件只能由 PhaseEngine 写入。
5. **校验顺序真实反映代码**：PhaseEngine 先做 phase 级 schema/范围校验再写规范 phase 文件；完整模块归并后，L1 才检查条款总数、执行轨迹和证据完整性。不通过者不得成为 report/audit 输入。
6. **回归条件**：以 5.6-6 重跑，验证 Agent 可检索 `16-CodeMin` 并返回 1 条 EvidenceManifest；不产生 Agent 直写的最终 `pre-Mx*.json`；PhaseEngine 生成规范 phase 文件与主 evidence；仅在完整 M1 的全部 phase 已归并、模块级 L1 通过后，才允许送 M1 Auditor。

### 纠正后的受控回归结果（2026-08-19，非正式）

当前实现已进一步把纯概念性条款前置到 M0 的独立 `m0_concept` 执行面，只暴露 `ixit_readonly`：

- `read_ixit_table`：按表名读取并返回 JSON Pointer、offset、总长度；
- `read_ics_clause`：按条款读取 ICS 声明；
- `search_ixit`：跨多个 IXIT 表受限检索。

以 5.6-6 单条回归时，Work Agent 仅调用这些只读工具，返回合格的 `EvidenceManifest`；随后由编排器归并并交给 M0 的 L1/L2 闸门。这个结果证明了**受限检索 → Agent 撰写 manifest → 编排器正式归档**的交接可行。

但该回归工作区只含 5.6-6；M1 模块 L1 要求完整 M1 条款集合，因此它按设计不会获得模块 L1 通过、更不会产生 `.evidence_M1_complete` 或 `.audit_M1_ACCEPTED`。这是范围控制，不是证据结构失败。

### 相关非正式产物（不得作为认证证据）

- `experiments/2026-08-19-m0-audit-probes/deepseek-flash-audit/audit-results/audit-result_M0.json`
- `experiments/2026-08-19-concept-lane-probes/flash/evidence/pre-M1-5.6-6-evidence.json`（历史回归，当前概念条款已不在 M1 phase_C 执行）

## 2026-08-19：完整 M0 概念链路的网关兼容与审计留存

### 实测发现

以相同 XLSX 在 `experiments/2026-08-19-m0-concept-deepseek/attempt-03-targeted-rework` 运行完整 M0：12 条概念条款均由受限 IXIT/ICS 工具完成，L1 通过；首轮 L2 产出有效 `REJECT`，只点名 6 条进入定向重评估。

随后暴露出四项实现问题：

1. `5.10` 同时有框架条款 ID 与测试案例 ID (`5.10-1-1`)，模型曾把后者写入正式 `clauseId`；已在任务包中强制区分。
2. DeepSeek Anthropic 兼容层可能把工具调用 DSML 信封作为纯文本返回；已增加一次**不带工具 schema**的恢复请求，且绝不执行文本中的伪工具调用。
3. `audit-agent.md` / `audit-output-schema.json` 仍是旧版扁平 `GOOD/NEEDS` 合约，与运行时 `AuditResult` 的嵌套 `ACCEPT/REJECT/FLAGGED` 合约冲突；已按 `contracts/audit.py` 统一。
4. M0 原先把 XLSX 中 `M F (...)` 条件条款的 `N/A` 当作无条件 M 的 FAIL；已改为由概念评估/L2 对照适用性理由，M0 仅执行确定性 support 形态检查。

### 留存原则

审计重试不能只保留最新 `audit-result_M0.json`。现已在每轮同时冻结
`audit-results/revisions/M0/round-N.json`，避免下一轮覆盖上一轮的 findings；evidence 的
`evidence/revisions/M0/revision-N.json` 继续保存被打回前的 manifest。

### 当前结论

首轮 `REJECT → 6 条定向返工 → L1` 已真实跑通。第二轮 DeepSeek 返回未满足 JSON 合约的分析文本，框架将其安全降级为 `FLAGGED`，只发 `.audit_M0_REVIEW_REQUIRED`，**没有**发 `.audit_M0_ACCEPTED`。这证明阻断链路有效；也说明对该网关仍需继续收紧结构化输出策略后，才可把它用于正式 L2 通行裁决。

## 2026-08-20：62 条概念全量运行的输出上限、字段契约与读全性问题

以同一 XLSX 把 M0 概念目录从早期 12 条扩到全量 62 条，`scratch/run_m0_concept_real.py` 完整跑 M0→Concept→L1→L2 闭环。首跑在 5.4-1（第 26 条）崩溃；修复后全量跑通（`concept_clause_count=62`、L1 通过、L2 经 3 轮审计仍 `REJECT`）。过程中暴露五个问题，逐一记录如下。

### 问题一：输出上限截断导致整条 run 崩溃

**触发条件**：`run_m0_concept_real.py` 用 `os.environ.setdefault("ANTHROPIC_MAX_TOKENS", "4096")` 硬设输出上限；5.4-1 需读 `10-SecParam`(11 行/13.6KB)+`1-AuthMech`(20 行/27.4KB) 双表并做 6 段 test_units 的复杂裁决，其 EvidenceManifest 超过 4096 token，JSON 在末尾被截断。

**根因**：
1. 4096 远小于框架默认 32000，也小于复杂条款实际需求；
2. `agent_runner.py` 解析最终 JSON 用 `except: pass` 静默吞掉失败，`output` 置 `None` 无任何日志；
3. `execute_conceptual_clause` 对 `result.output is None` 直接 `raise ValueError`，无逐条重试，一条 flaky 就杀死整批。

**风险**：长条款（多表/多行/多判据）必然触发；一次失败即中断 62 条全量，无局部恢复。

**纠正**：上限 4096→16384；`execute_conceptual_clause` 对空输出重试 2 次并附加"精炼输出"提示；最终失败时带上 `raw_text` 长度以便诊断。

**回归条件**：以 5.4-1 单条重跑，应返回合法 EvidenceManifest 而非 `output=None`；全量 62 条不因单条结构化输出失败而中断。

### 问题二：summary 字段命名错位（camelCase / snake_case 混用）

**触发条件**：跑通后 `concept_clause_count` 恒为 0（应为 62）、`audit.retry_count` 恒 None、`audit.retry_targets` 恒 `[]`。

**根因**：`FileBus.write_evidence` / `write_audit_result` 用 `model_dump(mode="json")`（未传 `by_alias=True`），落盘是 **snake_case** 字段名（`clause_id`、`retry_count`、`retry_instruction`、`target_clause_ids`）；而 summary 读取处写的是 **camelCase**（`clauseId`、`retryCount`、`retryInstruction`、`targetClauseIds`）。契约文件 `contracts/evidence.py`/`audit.py` 用 alias 同时声明两套，但序列化口径与读取口径不一致。

**风险**：这类"静默错值"最危险——结果结构完整、不报错，但关键统计字段全错，容易被当成正常输出。

**纠正**：读取处统一改 snake_case。**遗留**：应统一序列化口径（要么全程 `by_alias=True`，要么全程 snake_case），避免再次错位。

### 问题三：概念条款"读行读不全"（系统性，审计能抓但返工修不掉）

**触发条件**：L2 审计 `evidence_insufficient` finding 点名 4-1"仅检索 3 个 ICS 条目"、5.3-12"有 2 行未读取"。

**根因**：
1. `max_tool_turns = max(2, len(ixit_tables)+1)` 太紧——4-1 是 0 表文档条款却要扫全 78 条 ICS，只给 2 轮，物理读不完；
2. `read_ixit_table` 默认 8000 字符/次、`search_ixit` 只回 8 条，大表/大列表被截断；
3. work-agent 指令没有"检查 total_chars/offset、截断必须翻页读完"的铁律，模型读到第一段就下结论；
4. `read_ics_clause` 是"按 clause_id 子串过滤"的工具，**没有**读全量 ICS 的口子。

**风险**：漏读一行矛盾（如"Automatic upgrade not supported"）就可能把 FAIL 判成 PASS；且重跑用同一预算，返工也无法收敛——这正是本轮 3 轮审计仍 REJECT 的原因。

**纠正**：轮数 `max(3, len+2)`；单次读取 16000/32000；指令加翻页铁律 + 中文输出；新增 `read_ics_list`（32000）读全量 ICS。**验证**：4-1 从"读 689 字符/3 条"→"读满 47213 字符/枚举 10 个 N/A/抓出 5.5-7 与 5.12-3 两处 N/A 滥用"，由 INCONCLUSIVE 收敛为有实锤的 FAIL。

### 问题四：work-agent 输出语言不一致

**触发条件**：同一批 evidence，4-1 的 reason/warnings 是中文，5.1-1、5.1-2 却是全英文。

**根因**：work-agent 指令未强制输出语言，模型随 prompt/上下文在英中之间漂移。

**纠正**：`_CONCEPT_FRAMEWORK` 增加铁律——reason/warnings/expected/actual 一律中文，仅保留条款 ID、IXIT 表名、JSON Pointer、技术术语及被引用的 ICS/IXIT 原文为英文。

### 问题五：前端展示硬截断丢信息

**触发条件**：HTML 全景生成器 Python 侧 `_trunc` 把 reason 砍到 220 字符、warning 砍到 160、evidence 描述砍到 140，长分析被无谓丢弃。

**纠正**：去掉 Python 侧截断，全文进页面；前端用 CSS 两行折叠 + "展开/收起"按钮（超出才显示按钮）；新增"条款原文"展示。

### 本批纠正原则与回归条件

1. **结构化输出可靠性**：兼容网关（DeepSeek）的最终 JSON 是概率性的，输出上限要够宽、解析失败要可观测（带 raw_text 长度）、单条失败要可重试，三者缺一不可。
2. **序列化口径唯一**：`model_dump` 与读取处必须同口径，禁止"落盘 snake_case、读取 camelCase"。
3. **读全性靠工具+预算+指令三管齐下**，不靠模型自觉；文档类条款必须提供全量读取工具。
4. **回归**：全量 62 条重跑，`concept_clause_count==62`、`l1_passed==true`、审计 finding 中 `evidence_insufficient` 应显著减少，4-1 由 INCONCLUSIVE 转为确定裁决。

### 相关非正式产物（不得作为认证证据）

- 崩溃工作区 `D:/Auto-TEST/m0-concept-62-20260820`（`run_error.json`：`5.4-1: Agent 未返回有效 EvidenceManifest`）
- 修复后工作区 `D:/Auto-TEST/m0-concept-62-20260820-r2`（62/62 完成，L2 `REJECT`，27 findings / 12 retry_targets）
- 单条回归 `D:/Auto-TEST/m0-single-4-1-test*`（验证 read_ics_list 收敛）
